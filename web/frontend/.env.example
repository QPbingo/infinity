# API base URL. Empty in dev (Vite proxy makes /api same-origin).
# In production, set to the daemon origin, e.g. https://api.example.com
VITE_API_BASE=

# SSE stream path (relative to VITE_API_BASE). Always /api/events/stream.
# Provided as a variable only for flexibility; normally leave default.
